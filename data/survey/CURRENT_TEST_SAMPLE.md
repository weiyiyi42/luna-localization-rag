# Current Test Sample Set

The current survey/LUNA test sample set is:

- `data/survey/survey_items_100.csv`
- `data/survey/survey_items_100.json`
- `data/survey/survey_items_100_report.md`

This set follows Scheme C, excludes `General` and `Achievements`, and uses the all-TextAsset aligned corpus as its source.
